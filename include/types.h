#ifndef TYPES_H
#define TYPES_H

/*
** Shared type definitions for the Codexion simulation.
** The concrete definitions are introduced in milestone M2 (data model).
*/

typedef struct s_simulation	t_simulation;
typedef struct s_coder		t_coder;
typedef struct s_dongle		t_dongle;

#endif
